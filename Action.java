/*
 * This class stores enumerated actions (LEFT, RIGHT, UP, and DOWN), which is
 * stored in Nodes and used to alter states when using A* Search.
 */

public enum Action
{
    LEFT, RIGHT, UP, DOWN
}
